name: typos

on:
  push:

permissions:
  contents: read

jobs:
  typos:
    name: typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: typos
        uses: crate-ci/typos@v1.49.0
